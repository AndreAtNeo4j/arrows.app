<idea-plugin>
    <id>app.arrows.intellij</id>
    <name>Arrows</name>
    <vendor url="https://neo4j.com/labs/arrows">Neo4j Labs</vendor>
    <description><![CDATA[
        <p>The arrows.app canvas, inside your JetBrains IDE. Open a <code>.arrows</code> file and you get
        the editor from arrows.app — drag nodes, draw relationships, edit inline. The file stays plain
        JSON, so your graph models can live in git next to the code that uses them.</p>
        <p><b>Features</b></p>
        <ul>
          <li>Canvas editor for <code>.arrows</code> files</li>
          <li>Cypher export (copy or save), SVG export, and GraphQL-schema export</li>
          <li>Open the current graph in arrows.app, or import one from an arrows.app share link</li>
          <li>Project-wide rename for labels and relationship types</li>
          <li>Validate graph structure (duplicate ids, broken references, missing required fields)</li>
          <li>Sidebar with your workspace's <code>.arrows</code> files plus six bundled examples</li>
        </ul>
        <p>Plain JSON, the same format arrows.app and the VS Code extension use — the file opens
        interchangeably and diffs cleanly in git.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <fileType
            name="Arrows"
            implementationClass="app.arrows.intellij.ArrowsFileType"
            fieldName="INSTANCE"
            extensions="arrows"/>
        <fileEditorProvider implementation="app.arrows.intellij.ArrowsFileEditorProvider"/>
        <toolWindow id="Arrows" anchor="left" icon="/icons/arrows.svg" factoryClass="app.arrows.intellij.ArrowsToolWindowFactory"/>
    </extensions>
</idea-plugin>
